<UserControl x:Class="PiggsCare.UI.Views.NotificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:icons="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:PiggsCare.Core.ViewModels;assembly=PiggsCare.Core"
             d:DataContext="{d:DesignInstance Type=viewModels:NotificationViewModel}"
             d:DesignHeight="300"
             d:DesignWidth="300"
             mc:Ignorable="d">
    <Border Background="{DynamicResource SecondaryBackgroundColor}" CornerRadius="8">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition />
            </Grid.RowDefinitions>

            <!--  The label of the view  -->
            <Label HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Content="Notification"
                   FontSize="50"
                   FontWeight="Bold"
                   Foreground="{DynamicResource PrimaryTextColor}" />

            <Button Grid.Row="1" Command="{Binding ClearCommand}">
                <icons:Material Kind="NotificationClearAll" />
            </Button>


            <ListBox Grid.Row="2"
                     Background="Transparent"
                     ItemsSource="{Binding Messages}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Text="{Binding}" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Border>
</UserControl>